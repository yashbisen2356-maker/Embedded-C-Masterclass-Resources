Learned About Structure :
      =>  Hpw it is use to create user defined data types 
      => And how the sizeof the data in memory varies and gets actually stored in packed and unpacked format  
     => How we can use "__attribute__((packed)) " to packed the data and store memory but it eventually increases the complexity of the program. and workload on cpu
    => Practiced these programs and stored in code1, code2 and project1.